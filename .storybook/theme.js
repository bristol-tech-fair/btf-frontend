import { create } from '@storybook/theming/create';
import { themes } from '@storybook/theming';

const theme = create({
  brandTitle: 'BTFDS',
  brandUrl: 'https://www.bristoltechfair.org/',
  base: 'light'
});

export default theme;
