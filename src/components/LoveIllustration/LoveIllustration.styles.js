import styled from 'styled-components';

export const LoveIllustrationWrapper = styled.div`
  padding: var(--small-space);
  border: 1rem solid var(--lightblue);
  border-left: 1.2rem solid var(--lightblue);
  border-right: 1.2rem solid var(--lightblue);
  border-radius: 1.5rem;
`;
